<Window x:Class="PrivacyBrowser.App.PassphraseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Identity passphrase" Height="330" Width="500" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" Background="{StaticResource WindowBackground}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeadingText" FontSize="22" FontWeight="SemiBold" />
        <TextBlock x:Name="DescriptionText" Grid.Row="1" Margin="0,7,0,17" TextWrapping="Wrap"
                   Style="{StaticResource SecondaryText}" />

        <StackPanel Grid.Row="2">
            <TextBlock x:Name="PassphraseLabel" Text="Passphrase" FontWeight="SemiBold" Margin="0,0,0,5" />
            <PasswordBox x:Name="PassphraseBox" AutomationProperties.Name="Identity passphrase" />
        </StackPanel>

        <StackPanel x:Name="ConfirmationPanel" Grid.Row="3" Margin="0,13,0,0">
            <TextBlock Text="Confirm passphrase" FontWeight="SemiBold" Margin="0,0,0,5" />
            <PasswordBox x:Name="ConfirmationBox" AutomationProperties.Name="Confirm identity passphrase" />
        </StackPanel>

        <TextBlock x:Name="ValidationText" Grid.Row="4" Foreground="{StaticResource Danger}"
                   TextWrapping="Wrap" Margin="0,12,0,0" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Click="CancelButton_Click" Style="{StaticResource NeutralButton}" IsCancel="True" />
            <Button x:Name="ContinueButton" Content="Continue" Click="ContinueButton_Click" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
